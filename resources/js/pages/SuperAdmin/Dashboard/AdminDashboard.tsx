import React from "react";
import { Card, Space, Typography, Empty } from "antd";

const AdminDashboard: React.FC = () => {
    return (
        <Card title="Dashboard" className="w-100">
            <Empty />
        </Card>
    );
};

export default AdminDashboard;
